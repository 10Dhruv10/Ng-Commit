# ServicesDeepDive

## What I Learned

Advanced services with:

- **Signals** - `signal()`, `.update()`, `.set()`, `.asReadonly()`
- **Computed signals** for derived state
- **Service-to-service injection** (LoggingService → TasksService)
- **TypeScript interfaces** and type safety

## How to Use

Explore the service patterns:
- `src/app/tasks/tasks.service.ts` - Signals in services
- `src/app/logging.service.ts` - Service injection example
- `src/app/tasks/tasks.component.ts` - Component using services
- `src/app/tasks/task.model.ts` - TypeScript type definitions
